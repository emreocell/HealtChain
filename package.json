{
  "name": "ai-healthcare-decision-support",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Security-first research prototype for blockchain-backed medical record integrity and AI-assisted clinical education.",
  "scripts": {
    "start": "node apps/api/src/server.js",
    "dev": "node --watch apps/api/src/server.js",
    "chain": "hardhat node",
    "build": "hardhat build",
    "compile": "hardhat build",
    "deploy:local": "hardhat run scripts/deploy.ts --build-profile production --network localhost",
    "test": "npm run test:api && npm run test:contracts",
    "test:api": "node --test apps/api/test/*.test.js",
    "test:contracts": "hardhat test nodejs",
    "check": "node --check apps/api/src/config.js && node --check apps/api/src/validation.js && node --check apps/api/src/clinicalEducation.js && node --check apps/api/src/app.js && node --check apps/api/src/server.js && node --check apps/web/assets/app.js && hardhat build",
    "audit:runtime": "npm audit --omit=dev --audit-level=high",
    "audit:critical": "npm audit --audit-level=critical"
  },
  "keywords": [
    "healthcare",
    "blockchain",
    "solidity",
    "medical-records",
    "decision-support",
    "openai",
    "research"
  ],
  "author": "Emre Öcel",
  "license": "MIT",
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "5.0.7",
    "hardhat": "3.15.0"
  },
  "dependencies": {
    "cors": "2.8.6",
    "dotenv": "17.4.2",
    "express": "5.2.1",
    "openai": "7.10.0"
  }
}

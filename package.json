{
  "name": "ai-healthcare-decision-support",
  "version": "2.0.0",
  "private": true,
  "description": "Security-first research prototype for blockchain-backed medical record integrity and AI-assisted clinical education.",
  "scripts": {
    "start": "node apps/api/src/server.js",
    "dev": "node --watch apps/api/src/server.js",
    "chain": "hardhat node",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test": "npm run test:api && npm run test:contracts",
    "test:api": "node --test apps/api/test/*.test.js",
    "test:contracts": "hardhat test",
    "check": "node --check apps/api/src/config.js && node --check apps/api/src/validation.js && node --check apps/api/src/clinicalEducation.js && node --check apps/api/src/app.js && node --check apps/api/src/server.js && node --check apps/web/assets/app.js && node --check scripts/deploy.js"
  },
  "keywords": [
    "healthcare",
    "blockchain",
    "solidity",
    "medical-records",
    "decision-support",
    "openai",
    "research"
  ],
  "author": "Emre Öcel",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "openai": "^5.8.2"
  }
}
